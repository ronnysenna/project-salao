import { Typography } from '@mui/material';

const Section2 = ({ content }) => {
  return (
    <section>
      <Typography>
        <div class="container">
          <div class="feature">
            <h2>Feature 3</h2>
            <p>of feature 3</p>
          </div>
        </div>
      </Typography>
    </section>
  );
};

export default Section2;
