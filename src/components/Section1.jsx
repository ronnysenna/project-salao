import { Typography } from '@mui/material';

const Section1 = ({ content }) => {
  return (
    <section>
      <Typography>
        <div class="container">
          <div class="feature">
            <h2>Feature 2</h2>
            <p>of feature 2</p>
          </div>
        </div>
      </Typography>
    </section>
  );
};

export default Section1;
