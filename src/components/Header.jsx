import { Typography } from "@mui/material";

const Nav = () => {
  return (
    <nav>
      <Typography>
        <div>
          <section>
            <div>
              <h1>Salão de Beleza</h1>
              <button>Agendar</button>
            </div>
          </section>
        </div>
      </Typography>
    </nav>
  );
};

export default Nav;
